# template

```
claude run -p "read llm-instrutions.md, interpret its contents, and execute the requested workflow."
```
```
claude run -p "read llm-instructions.md, interpret its contents, and enter knowledge base mode."
```
